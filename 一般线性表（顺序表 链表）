C风格，使用构造函数
#include<iostream>
using namespace std;

struct node{      
	int data;
	node *next;
}Node;

node *create() {          
	node *head, *curr;
	head = new node;
	head->next = NULL;				
	curr = head;
	while (cin >> Node.data) {
		curr->next = new node;
		curr = curr->next;
		curr->data = Node.data;
	}
	curr->next = NULL;
	return head;
}

int main() {
	node *link=create();        
	while (link->next)            
	{
		cout << link->next->data << endl;
		link = link->next;
	}
}

C风格，使用insert操作创建链表



//模板






//STL






//相关题目
